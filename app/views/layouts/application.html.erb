<!DOCTYPE html>
<html>
<head>
  <title>GameContestServer</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    
    <div class="masthead">
      <h3 class="text-muted">Game Contest Server</h3>
      <nav class="navbar navbar-default" role="navigation">
        <ul class="nav nav-justified">
          <li class="active"><a href="/signup">Homepage</a></li>
          <li><%= link_to "Game Contest Server", root_path %></li>
          <li><%= link_to "Users", users_path %></li>
          
          <% if logged_in? %>
          <li id="fat-menu" class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log Out", logout_path, method: "delete" %>
              </li>
            </ul>
          </li>
          <% else %>
          <li><a href="/signup">Sign Up</a></li>
          <li><a href="/login" class="btn btn-primary btn-large"><i class="icon-white icon-ok"></i>Log In</a></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
  
	<% flash.each do |level, message| %>
		<div class ="alert alert-<%= level %>"><%= message %></div>
	<% end %>
	
	<div class="container">
		<%= yield %>
	</div>

</body>
</html>
